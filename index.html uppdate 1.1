<!DOCTYPE html>
<html lang="no">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Klikker Spillet: Verdensrommet</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #1a1a2e; /* Mørk bakgrunn som representerer verdensrommet */
            color: white;
        }
        h1 {
            font-size: 2.5em;
            margin-top: 20px;
        }
        #score {
            font-size: 2em;
            margin-top: 20px;
        }
        #click-button {
            font-size: 1.5em;
            padding: 20px;
            margin-top: 20px;
            cursor: pointer;
            background-color: #ff6347; /* Romskip knapp-farge */
            color: white;
            border: none;
            border-radius: 10px;
            transition: 0.3s;
        }
        #click-button:hover {
            background-color: #ff4500;
        }
        #upgrade-button {
            font-size: 1.2em;
            padding: 10px;
            margin-top: 20px;
            cursor: pointer;
            background-color: #8a2be2; /* Butikk knapp-farge */
            color: white;
            border: none;
            border-radius: 10px;
            transition: 0.3s;
        }
        #upgrade-button:hover {
            background-color: #7a1dbd;
        }
        .shop {
            margin-top: 40px;
            font-size: 1.2em;
        }
        .shop img {
            width: 100px;
            height: 100px;
            margin: 10px;
        }
        .shop-item {
            margin: 20px;
            text-align: center;
        }
        .shop-item button {
            padding: 10px 20px;
            background-color: #4caf50;
            color: white;
            font-size: 1.2em;
            border: none;
            border-radius: 10px;
            cursor: pointer;
        }
        .shop-item button:hover {
            background-color: #45a049;
        }
        .planet {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background-color: #4b0082;
            margin: 10px auto;
        }
        .auto-clicker {
            font-size: 1.2em;
            color: #ffcc00;
        }
    </style>
</head>
<body>

    <h1>Verdensrom Klikker-spillet!</h1>
    <p id="score">Poeng: 0</p>
    
    <!-- Klikk knapp med romskip bakgrunn -->
    <button id="click-button">
        🚀 Klikk på romskipet!
    </button>

    <div class="button-container">
        <button id="upgrade-button">Kjøp Oppgradering: 10 poeng</button
<!DOCTYPE html>
<html lang="no">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Klikker Spillet: Verdensrommet</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #1a1a2e; /* Mørk bakgrunn som representerer verdensrommet */
            color: white;
        }
        h1 {
            font-size: 2.5em;
            margin-top: 20px;
        }
        #score {
            font-size: 2em;
            margin-top: 20px;
        }
        #click-button {
            font-size: 1.5em;
            padding: 20px;
            margin-top: 20px;
            cursor: pointer;
            background-color: #ff6347; /* Romskip knapp-farge */
            color: white;
            border: none;
            border-radius: 10px;
            transition: 0.3s;
        }
        #click-button:hover {
            background-color: #ff4500;
        }
        #upgrade-button {
            font-size: 1.2em;
            padding: 10px;
            margin-top: 20px;
            cursor: pointer;
            background-color: #8a2be2; /* Butikk knapp-farge */
            color: white;
            border: none;
            border-radius: 10px;
            transition: 0.3s;
        }
        #upgrade-button:hover {
            background-color: #7a1dbd;
        }
        .shop {
            margin-top: 40px;
            font-size: 1.2em;
        }
        .shop img {
            width: 100px;
            height: 100px;
            margin: 10px;
        }
        .button-container {
            margin-top: 20px;
        }
        .planet {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            background-color: #4b0082;
            margin: 10px auto;
        }
    </style>
</head>
<body>

    <h1>Verdensrom Klikker-spillet!</h1>
    <p id="score">Poeng: 0</p>
    
    <!-- Klikk knapp med romskip bakgrunn -->
    <button id="click-button">
        🚀 Klikk på romskipet!
    </button>

    <div class="button-container">
        <button id="upgrade-button">Kjøp Oppgradering: 10 poeng</button>
    </div>

    <div class="shop">
        <h2>Rombutikk</h2>
        <p>Oppgrader romskipet ditt!</p>
        <img src="https://via.placeholder.com/100x100/FF6347/FFFFFF?text=Rocket" alt="Romskip">
        <div class="planet"></div>
        <img src="https://via.placeholder.com/100x100/8A2BE2/FFFFFF?text=Planet" alt="Planet">
    </div>

    <script>
        let score = 0;
        let clickPower = 1; // Starter med 1 poeng per klikk
        const scoreElement = document.getElementById("score");
        const clickButton = document.getElementById("click-button");
        const upgradeButton = document.getElementById("upgrade-button");

        // Funksjon for å oppdatere poengsummen
        function updateScore() {
            scoreElement.textContent = "Poeng: " + score;
        }

        // Klikk knappen
        clickButton.addEventListener("click", function() {
            score += clickPower;
            updateScore();
        });

        // Oppgraderingsknappen
        upgradeButton.addEventListener("click", function() {
            if (score >= 10) {
                score -= 10;
                clickPower += 1; // Øk poeng per klikk
                updateScore();
                upgradeButton.textContent = "Kjøp Oppgradering: " + (10 * (clickPower)) + " poeng";
            } else {
                alert("Du trenger 10 poeng for å kjøpe oppgradering!");
            }
        });

        // Start oppgraderingstekst
        upgradeButton.textContent = "Kjøp Oppgradering: 10 poeng";
    </script>

</body>
</html>
